# RAMUSE（子供用アプリ）— 案件ノート

ステータス：作業中（新アプリへ切替済み・2026-09-12）。単体では編集しない。

## この案件は何か

RAMUSEの子供用アプリ。アイリ・ミハ・リンカが見る。大会の予定・当日情報・持ち物・結果・カレンダーだけを出し、**お金とやることは出さない**。

## 重要：このリポジトリのファイルは編集しない

- ソースは大人用リポジトリの `../ramuse-deploy/web/` にある1本だけ。ここにあるのは**ビルド結果**
- 直し方：`cd ../ramuse-deploy/web && npm run build` → 大人用と子供用の両方をcommit・push
- `app/` は旧い共有URLの転送ページ
- `index.html`（旧・子供用アプリ）は 2026-09-12 のビルドで置き換わった。旧版はGitの履歴にある

## 公開URL

https://ramuse-lock.github.io/ramuse-kid/

## 参照

詳細・決めごと・落とし穴は `../ramuse-deploy/AGENTS.md` を見る（両アプリで共通）。
